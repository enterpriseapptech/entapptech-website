import CompanyPageUI from "@/components/page/CompanyPageUI";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ENTAPP TECH | Company",
};

const Company = () => {
  return <CompanyPageUI />;
};

export default Company;
